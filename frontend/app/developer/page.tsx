import { CaseLookup } from "@/components/case-lookup";
import { LiveContractNotice } from "@/components/live-contract-notice";
import { WalletControl } from "@/components/wallet";

export default function DeveloperPage() {
  return <div className="page-shell developer-page"><header className="page-intro page-intro-split"><div><p className="kicker">Developer workspace / 02</p><h1>Ship the fix.<br /><em>Prove the commit.</em></h1><p>Open your assigned case, inspect the immutable brief, then submit the exact commit SHA. PatchBond retrieves and authenticates the evidence itself.</p></div><div className="developer-badge"><span className="badge-eyebrow">DEVELOPER MODE</span><strong>Code in.</strong><strong>URLs out.</strong><span>Only repository-bound commits enter review.</span></div></header><div className="developer-grid"><section className="developer-entry"><div className="section-line"><div><p className="kicker">Case access</p><h2>Open a remediation brief</h2></div><span className="section-index">PUBLIC READ / WALLET WRITE</span></div><LiveContractNotice /><WalletControl /><CaseLookup /><div className="principle-strip"><span className="principle-icon">&#8982;</span><div><strong>No uploaded proof.</strong><span>No arbitrary URLs, screenshots, or mutable branches. Only bound repository commits enter adjudication.</span></div></div></section><aside className="developer-aside"><p className="kicker">Before you accept</p><h2>Read the terms like a security review.</h2><ol><li><b>01</b><span>Confirm the base commit is the version you are patching.</span></li><li><b>02</b><span>Check the acceptance criteria is observable in code.</span></li><li><b>03</b><span>Submit only the final, exact 40-character SHA.</span></li></ol></aside></div></div>;
}
